# Local Sync

Recommended generic sync mode:

```text
Windows -> servidor: send only
Servidor -> Windows: send only
```

Keep Syncthing device IDs, folder IDs, private paths, and credentials out of Git. Use local override files or external configuration.
