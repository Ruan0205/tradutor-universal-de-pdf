# Translation Memory

The database schema includes a translation memory table keyed by source text hash. The next implementation step is to consult this table before provider calls and write accepted translations after validation.
