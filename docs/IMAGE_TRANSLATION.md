# Image Translation

Image translation is represented in Document IR and staged in the pipeline. The current safe baseline preserves images and avoids external services by default. Neural inpainting and external image OCR/translation must remain optional and explicitly enabled.
