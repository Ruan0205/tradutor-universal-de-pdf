# Tables

The current production layer preserves a table stage in the durable pipeline and Document IR schema. Advanced table structure reconstruction is still behind the `TableProvider` interface target and must be benchmarked with generated/public PDFs before being used for production books.
